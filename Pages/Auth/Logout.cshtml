@page
@model Plantpedia.Pages.Auth.LogoutModel
@{
    Layout = null;
    ViewData["Title"] = "Logout";
}

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="card">
                <div class="card-body text-center">
                    <h2 class="card-title">Đăng xuất thành công</h2>
                    <p class="card-text">Bạn đã đăng xuất khỏi hệ thống.</p>
                    <a href="/Auth/Login" class="btn btn-primary">Đăng nhập lại</a>
                </div>
            </div>
        </div>
    </div>
</div>

